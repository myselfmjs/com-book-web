1、Interface 中可以定义变量 必须是 public static final 类型的
2、调用Interface 中变量的方法  A.Interface.变量 B.实现类.变量 C.实现类的实体.变量
3、接口可以继承接口
4、抽象类可以实现接口
5、抽象类可以继承实体类，不过实体类必须有明确的构造函数；也就是父类可以被继承，并且拥有子类可访问的构造器。
   不同实体类在继承的时候也应该满足该条件，否则也无法继承
